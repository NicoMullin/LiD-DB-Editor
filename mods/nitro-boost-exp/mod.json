{
  "id": "nitro-boost-exp",
  "name": "Nitro Boost EXP",
  "description": "Nitro Boost and Turbo-charged Engine give {{percent:comma}}% EXP instead of 40% / 10%, and their in-game descriptions are rewritten to say so.",
  "version": "3.0.0",
  "author": "KSFA",
  "game_version": ["5.0.3.0.0 - 1.87", "5.0.4.0.0 - 1.88"],
  "requires": [],
  "conflicts_with": [],
  "settings": [
    {
      "id": "percent",
      "label": "EXP bonus",
      "type": "integer",
      "default": 100000,
      "min": 1,
      "max": 1000000,
      "step": 1000,
      "unit": "%",
      "help": "Stock is 40% for Nitro Boost and 10% for Turbo-charged Engine. The descriptions are rewritten to match, in six languages."
    }
  ],
  "patches": [
    {
      "type": "update_set",
      "table": "master_skill",
      "set": {
        "val0": "{{percent}}"
      },
      "where": "id = 'SKL_EXPUP_02'",
      "expected_rows": 1
    },
    {
      "type": "update_set",
      "table": "master_skill",
      "set": {
        "val0": "{{percent}}"
      },
      "where": "id = 'SKL_EXPUP_01'",
      "expected_rows": 1
    },
    {
      "type": "raw_sql_file",
      "path": "descriptions.sql",
      "id": "text",
      "description": "Skill descriptions, so the tooltip matches the number"
    }
  ]
}
